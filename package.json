{
  "name": "redux-service-util",
  "version": "1.1.0",
  "description": "Utilities for building Redux data services",
  "main": "dist/index.js",
  "repository": "git@github.com:danny-wieser/redux-service-util.git",
  "author": "danny.wieser@gmail.com",
  "license": "MIT",
  "files": [
    "dist/*.*",
    "README.md"
  ],
  "scripts": {
    "build": "yarn run clean && tsc",
    "start": "yarn run clean && tsc -w",
    "clean": "rimraf dist coverage yarn-error.log",
    "lint": "tslint --project \".\"",
    "test": "jest --coverage --collectCoverageFrom=src/** && yarn run lint"
  },
  "devDependencies": {
    "@types/jest": "^23.3.8",
    "jest": "^23.5.0",
    "rimraf": "^2.6.2",
    "ts-jest": "^23.10.4",
    "tslint": "^5.11.0",
    "tslint-config-airbnb": "^5.11.0",
    "typescript": "^3.1.3"
  },
  "dependencies": {
    "whatwg-fetch": "^3.0.0"
  }
}
